# TestBase

**Category:** core
**Schema:** [`schema.json`](./schema.json)

## What it does

Trimmed analog of `specsheets/core/SEDBase`: contributes the optional `name`/`description` fields every test-specsheets/ class inherits via `allOf`. Synthetic generator-test fixture - see Design.md's Specification Assembly section.

## Attributes

| Attribute | Type | Required | Notes |
|---|---|---|---|
| `name` | string | no | |
| `description` | string | no | |
