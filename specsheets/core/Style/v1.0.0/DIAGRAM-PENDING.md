# Diagram pending

No UML diagram currently exists for **Style** in the source spec (`SED2.md`) or the legacy `sed/spec/UML/` folder. This is flagged here as a follow-up item rather than an invented diagram.
