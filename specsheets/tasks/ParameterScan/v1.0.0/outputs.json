{
  "outputs": {
    "[id]": {
      "valid": true,
      "type": "annotatedData",
      "dimensions": {
        "source": "static",
        "expr": "one dimension per entry in parameterRanges (each sized by that entry's own range.numberOfSteps), plus one further dimension per entry of outputVariableMap",
        "note": "dimensionality grows with the number of ranges scanned; each parameterRanges dimension's own size is itself a per-entry static formula (range.numberOfSteps)"
      }
    },
    "[id].model": {
      "valid": false
    },
    "[id].strings": {
      "valid": false
    },
    "[id].aggregates": {
      "valid": "provided(aggregateOutputVariables)",
      "type": "annotatedData",
      "dimensions": [
        {
          "size": {
            "source": "static",
            "expr": "len(aggregateOutputVariables)"
          },
          "labels": null
        }
      ]
    }
  }
}
